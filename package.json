{
  "type": "module",
  "name": "letonikabc",
  "version": "1.0.0",
  "description": "Tīmekļa vietne, kurā lietotājs lejupielādē teksta failu no letonika.lv vietnes un izmanto kā alternatīvu lasītāvu",
  "main": "index.js",
  "scripts": {
    "dev": "wasm-pack build crates/parser --debug --target web && vite",
    "build": "bun run pack-wasm && vite build",
    "preview": "bun run build && vite preview",
    "extract": "bun run build && wasm2wat crates/parser/pkg/parser_bg.wasm -o crates/parser/pkg/release.wat",
    "opt": "wasm-opt -Oz crates/parser/pkg/parser_bg.wasm -o crates/parser/pkg/parser_bg.wasm --enable-bulk-memory --enable-nontrapping-float-to-int --strip-debug --strip-dwarf --strip-producers --strip-target-features",
    "test": "cargo test --target x86_64-pc-windows-msvc",
    "pack-wasm": "wasm-pack build crates/parser --release --target web && bun run opt"
  },
  "keywords": [
    "letonika",
    "reader",
    "rust",
    "wasm"
  ],
  "author": "robertpakalns",
  "dependencies": {
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.2",
    "react": "^19.2.3",
    "vite": "^7.3.0"
  }
}
